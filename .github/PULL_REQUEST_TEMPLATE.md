<!-- 2-4 lines: what changed and why. -->

<!-- Verified (optional): only list checks CI can't run, e.g. a live curl,
     `tofu plan` output summary, `nix flake check` on the host. Delete this
     comment and the section below if CI alone covers verification. -->
